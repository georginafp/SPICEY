^.*\.Rproj$
^\.Rproj\.user$
^Meta$
^_pkgdown\.yml$
^pkgdown$
# ^doc$          # Commented out to avoid interfering with vignette output
# ^docs$         # Commented out for same reason; keep if you use docs/ for pkgdown site
